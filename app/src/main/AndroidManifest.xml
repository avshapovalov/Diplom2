<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andrognito.notesfordiplom">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.andrognito.notesfordiplom.NewNote"></activity>
        <activity android:name="com.andrognito.notesfordiplom.Notes" />
        <activity android:name="com.andrognito.notesfordiplom.SettingsActivity" />
        <activity android:name="com.andrognito.notesfordiplom.PinCodeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>